import type { Signal, Integration } from "../domain";

/** Connections and service observations appear only after they are configured. */
export const integrations: Integration[] = [];
export const signals: Signal[] = [];
